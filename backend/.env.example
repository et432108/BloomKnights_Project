# ---------------------------------------------------------------------------
# BloomKnights backend (Cloud Functions) environment
# SERVER-ONLY secrets. Never bundle into the app. In production these are set
# via Firebase secrets, not this file:
#   firebase functions:secrets:set GEMINI_API_KEY
# For local emulator runs, copy to .env and fill in.
# ---------------------------------------------------------------------------

GEMINI_API_KEY=
